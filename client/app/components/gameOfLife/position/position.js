class Position {
  /**
   * Position class
   * @param {integer} x - x position
   * @param {integer} y - y position
   */
  constructor(x = 0, y = 0) {
    this.x = x;
    this.y = y;
  }
}

export default Position;
